{
  "skill_name": "batch-stories",
  "evals": [
    {
      "id": 1,
      "prompt": "Implement these stories: GC-DOC-US031 GC-DOC-US019 GC-DOC-US020",
      "expected_output": "Parallel autonomous implementation of all 3 stories, each in an isolated worktree with its own branch and PR, with post-batch quality review",
      "expectations": [
        "The skill validates each story code exists in SmartSuite before launching",
        "The skill reports the list of stories that will be implemented with their titles",
        "The skill launches one Agent per story without isolation: worktree (autopilot Phase 1 creates the worktree at .worktrees/feature/STORY-ID)",
        "All agents are launched in a single message (parallel, not sequential)",
        "The skill locates the omc-rails-autopilot SKILL.md file before launching agents",
        "Each agent receives a prompt with the resolved skill file path to read directly (not invoke /omc-rails-autopilot)",
        "Each agent runs with run_in_background: true",
        "The skill sets up a monitoring loop after launching agents",
        "The skill reports a progress summary table showing story, status, branch, and PR columns",
        "Each completed story results in a separate git branch (not all changes on one branch)",
        "Each completed story results in a separate PR",
        "The skill offers to run a post-batch quality pass (/dhh-review and /bali-view-audit) on completed PRs",
        "SmartSuite story statuses are updated during execution"
      ]
    },
    {
      "id": 2,
      "prompt": "Batch autopilot next 3",
      "expected_output": "Fetches 3 stories from SmartSuite backlog and launches parallel agents in isolated worktrees",
      "expectations": [
        "The skill queries SmartSuite for the top 3 backlog stories scoped to the current project",
        "The skill reports which stories were found with their codes and titles before launching",
        "The skill launches 3 parallel agents (each creates its worktree via autopilot Phase 1)",
        "Each agent runs the full omc-rails-autopilot workflow independently",
        "The skill does not launch agents for stories that are not in backlog status"
      ]
    },
    {
      "id": 3,
      "prompt": "Implement these stories and let me review them tomorrow: GC-DOC-US031 GC-DOC-US019 GC-DOC-US020",
      "expected_output": "Launches 3 parallel agents with monitoring, designed for overnight autonomous execution",
      "expectations": [
        "The skill launches all 3 agents in parallel (single message, not sequential)",
        "Each agent creates its own worktree via autopilot Phase 1 at .worktrees/feature/STORY-ID",
        "The skill sets up a monitoring loop to track progress across all 3 stories",
        "The batch continues running without requiring user interaction",
        "Each story produces an independent PR that can be reviewed separately"
      ]
    }
  ]
}
